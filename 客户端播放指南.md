# Wayland 显示流媒体客户端播放指南

## 当前状态

✅ **服务器已启动**: GStreamer Wayland 显示服务器正在运行  
✅ **UDP 流已启动**: 端口 5000，H.264 编码，2000kbps 比特率  
✅ **Wayland socket 已创建**: `/run/user/1000/wayland-1`  

## 服务器信息

- **服务器 IP**: 需要替换为你的实际 IP 地址
- **UDP 端口**: 5000
- **编码格式**: H.264 (OpenH264)
- **分辨率**: 1280x720@30fps
- **比特率**: 2000kbps

## 客户端播放方法

### 方法 1: 使用 GStreamer 播放

```bash
# 在同一台机器上播放
gst-launch-1.0 udpsrc port=5000 ! application/x-rtp,encoding-name=H264,payload=96 ! rtph264depay ! h264parse ! openh264dec ! autovideosink

# 从远程机器播放 (替换 YOUR_SERVER_IP)
gst-launch-1.0 udpsrc host=YOUR_SERVER_IP port=5000 ! application/x-rtp,encoding-name=H264,payload=96 ! rtph264depay ! h264parse ! openh264dec ! autovideosink

# 或者使用客户端播放脚本（自动选择最佳解码器）
./client_play.sh udp
./client_play.sh udp YOUR_SERVER_IP
```

### 方法 2: 使用 VLC 播放

```bash
# 在同一台机器上播放
vlc rtp://@:5000

# 从远程机器播放 (替换 YOUR_SERVER_IP)
vlc rtp://YOUR_SERVER_IP:5000
```

### 方法 3: 使用 FFmpeg 播放

```bash
# 在同一台机器上播放
ffplay udp://localhost:5000

# 从远程机器播放 (替换 YOUR_SERVER_IP)
ffplay udp://YOUR_SERVER_IP:5000
```

### 方法 4: 使用 MPV 播放

```bash
# 在同一台机器上播放
mpv udp://localhost:5000

# 从远程机器播放 (替换 YOUR_SERVER_IP)
mpv udp://YOUR_SERVER_IP:5000
```

### 方法 5: 使用客户端播放脚本（推荐）

```bash
# UDP 流播放
./client_play.sh udp                    # 本地播放
./client_play.sh udp 192.168.6.60      # 远程播放

# TCP 流播放
./client_play.sh tcp                    # 本地播放
./client_play.sh tcp 192.168.6.60      # 远程播放

# VLC 播放
./client_play.sh vlc-udp               # UDP 流
./client_play.sh vlc-tcp 192.168.6.60  # TCP 流

# FFplay 播放
./client_play.sh ffplay-udp            # UDP 流
./client_play.sh ffplay-tcp 192.168.6.60 # TCP 流

# MPV 播放
./client_play.sh mpv-udp               # UDP 流
./client_play.sh mpv-tcp 192.168.6.60  # TCP 流
```

## 获取服务器 IP 地址

```bash
# 获取本机 IP 地址
ip addr show | grep "inet " | grep -v 127.0.0.1

# 或者使用
hostname -I
```

## 启动 Wayland 应用程序显示内容

要让流媒体显示实际内容，需要启动 Wayland 应用程序：

```bash
# 设置环境变量
export WAYLAND_DISPLAY=wayland-1

# 启动各种 Wayland 应用程序
weston-simple-egl    # 简单的 OpenGL 测试程序
weston-terminal      # 终端模拟器
firefox              # 浏览器（如果支持 Wayland）
gnome-terminal       # GNOME 终端（如果支持 Wayland）
```

## 其他流媒体选项

### TCP 流输出
```bash
cd /home/lzx/code/gst-wayland-display
./streaming_options.sh tcp
```

### RTMP 流输出
```bash
cd /home/lzx/code/gst-wayland-display
./streaming_options.sh rtmp
```

### 文件录制
```bash
cd /home/lzx/code/gst-wayland-display
./streaming_options.sh file
```

## 故障排除

### 1. 检查服务器状态
```bash
cd /home/lzx/code/gst-wayland-display
./streaming_options.sh status
```

### 2. 检查网络连接
```bash
# 检查端口是否在监听
netstat -tuln | grep 5000

# 或者使用 ss
ss -tuln | grep 5000
```

### 3. 检查防火墙设置
```bash
# 如果使用防火墙，需要开放端口 5000
sudo ufw allow 5000
# 或者
sudo iptables -A INPUT -p udp --dport 5000 -j ACCEPT
```

### 4. 测试网络连通性
```bash
# 从客户端测试 UDP 连接
nc -u YOUR_SERVER_IP 5000
```

## 性能优化

### 调整比特率
```bash
# 使用更高的比特率
BITRATE=4000 ./streaming_options.sh udp

# 使用更高的分辨率
WIDTH=1920 HEIGHT=1080 ./streaming_options.sh udp
```

### 使用硬件编码器（如果可用）
```bash
# 检查可用的硬件编码器
gst-inspect-1.0 | grep -E "(nvh264enc|vah264enc)"

# 使用 NVIDIA 硬件编码器
GST_PLUGIN_PATH=/usr/local/lib/gstreamer-1.0 gst-launch-1.0 \
    waylanddisplaysrc ! \
    "video/x-raw,width=1280,height=720,format=RGBx,framerate=30/1" ! \
    videoconvert ! \
    nvh264enc bitrate=2000 ! \
    rtph264pay ! \
    queue ! \
    udpsink host=0.0.0.0 port=5000
```

## 停止服务

```bash
cd /home/lzx/code/gst-wayland-display
./streaming_options.sh stop
```

## 注意事项

1. **网络延迟**: UDP 流可能有网络延迟，建议在同一局域网内使用
2. **防火墙**: 确保防火墙允许 UDP 端口 5000 的通信
3. **带宽**: 2000kbps 的比特率需要足够的网络带宽
4. **Wayland 客户端**: 只有连接到 `wayland-1` socket 的应用程序才会显示在流中
5. **硬件支持**: 如果系统支持硬件编码，可以获得更好的性能
