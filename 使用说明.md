# GStreamer Wayland 显示插件使用说明

## 概述

这个项目提供了一个 GStreamer 插件，可以创建一个微型的 Wayland 合成器，用于捕获和显示 Wayland 桌面内容。

## 启动步骤

### 1. 启动 GStreamer Wayland 显示服务器

```bash
# 设置插件路径并启动 GStreamer 管道
GST_PLUGIN_PATH=/usr/local/lib/gstreamer-1.0 gst-launch-1.0 waylanddisplaysrc ! 'video/x-raw,width=1280,height=720,format=RGBx,framerate=60/1' ! autovideosink
```

这个命令会：
- 启动一个 Wayland 合成器服务器
- 创建一个 Wayland socket（通常在 `$XDG_RUNTIME_DIR/wayland-1`）
- 开始捕获桌面内容并输出到视频流

### 2. 验证 Wayland 服务器是否启动

```bash
# 检查 Wayland socket 是否创建
ls -la $XDG_RUNTIME_DIR | grep wayland

# 应该看到类似这样的输出：
# srwxr-xr-x 1 user user 0 Sep 22 23:00 wayland-1
# -rw-r----- 1 user user 0 Sep 22 23:00 wayland-1.lock
```

### 3. 连接到 Wayland 服务器

一旦 Wayland 服务器启动，你可以启动任何 Wayland 客户端来连接到它：

```bash
# 设置环境变量指向我们的 Wayland 服务器
export WAYLAND_DISPLAY=wayland-1

# 启动各种 Wayland 应用程序
weston-simple-egl          # 简单的 OpenGL 测试程序
weston-terminal            # 终端模拟器
firefox                    # 浏览器（如果支持 Wayland）
gnome-terminal             # GNOME 终端（如果支持 Wayland）
```

## 工作原理

1. **Wayland 服务器**: GStreamer 插件启动一个基于 Smithay 的 Wayland 合成器
2. **Socket 通信**: 通过 Unix socket 在 `$XDG_RUNTIME_DIR/wayland-1` 进行通信
3. **桌面捕获**: 合成器捕获所有连接到它的 Wayland 客户端的内容
4. **视频输出**: 捕获的内容通过 GStreamer 管道输出为视频流

## 高级用法

### 零拷贝管道支持

插件支持 DMA 缓冲区以实现零拷贝管道：

```bash
# NVIDIA GPU
gst-launch-1.0 waylanddisplaysrc ! 'video/x-raw(memory:DMABuf),width=1920,height=1080,framerate=60/1' ! glupload ! glcolorconvert ! 'video/x-raw(memory:GLMemory), format=NV12' ! nvh265enc ! nvh265dec ! autovideosink

# AMD/Intel GPU
gst-launch-1.0 waylanddisplaysrc ! 'video/x-raw(memory:DMABuf),width=1920,height=1080,framerate=60/1' ! vapostproc ! 'video/x-raw(memory:VAMemory), format=NV12' ! vah265enc ! vah265dec ! autovideosink
```

### 自定义渲染节点

```bash
# 使用软件渲染
GST_PLUGIN_PATH=/usr/local/lib/gstreamer-1.0 gst-launch-1.0 waylanddisplaysrc render-node=software ! 'video/x-raw,width=1280,height=720,format=RGBx,framerate=60/1' ! autovideosink

# 使用特定的 DRM 节点
GST_PLUGIN_PATH=/usr/local/lib/gstreamer-1.0 gst-launch-1.0 waylanddisplaysrc render-node=/dev/dri/renderD129 ! 'video/x-raw,width=1280,height=720,format=RGBx,framerate=60/1' ! autovideosink
```

## 故障排除

### 常见问题

1. **插件未找到**
   ```bash
   # 检查插件是否正确安装
   GST_PLUGIN_PATH=/usr/local/lib/gstreamer-1.0 gst-inspect-1.0 waylanddisplaysrc
   ```

2. **DRM 节点访问权限问题**
   ```bash
   # 确保用户有访问 DRM 节点的权限
   sudo usermod -a -G video $USER
   # 然后重新登录
   ```

3. **Wayland 客户端无法连接**
   ```bash
   # 确保环境变量设置正确
   echo $WAYLAND_DISPLAY
   # 应该显示 wayland-1
   ```

### 调试信息

```bash
# 启用详细日志
RUST_LOG=debug GST_PLUGIN_PATH=/usr/local/lib/gstreamer-1.0 gst-launch-1.0 waylanddisplaysrc ! 'video/x-raw,width=1280,height=720,format=RGBx,framerate=60/1' ! autovideosink
```

## 注意事项

- 这个插件创建的是一个独立的 Wayland 服务器，不是连接到现有的 Wayland 会话
- 所有连接到这个服务器的 Wayland 客户端都会显示在 GStreamer 的输出中
- 确保在启动 Wayland 客户端之前先启动 GStreamer 管道
- 某些应用程序可能不支持 Wayland，会回退到 X11 模式
